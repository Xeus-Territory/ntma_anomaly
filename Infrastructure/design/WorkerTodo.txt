1. Orchestration Manage the swarm
- Make sure image can pull new inside the swarm when scale up or down
- Setup scaling plan by Logic from another module (AI + Data/Analytic)
- Trigger notify for notify telegram

2. Start up swarm
- Check swarm is running ? Make next step do : Turn it into swarm and go to next Setup
- Setup network for swarm
- Do start up with twice docker-compose file to setup application
- Do start up for component for monitoring and logging (Prometheus + Grafana)
- Trigger the module for notify telegram

3. log-process.py:
- Trigger 5 minute timeout for logging metric from nginx, logging for day
- Telegram module for trigger